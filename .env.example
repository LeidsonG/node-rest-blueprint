# Server
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL=postgresql://api_user:api_password@localhost:5432/node_rest_blueprint?schema=public

# JWT
JWT_SECRET=troque_por_um_secret_longo_e_aleatorio_em_producao
JWT_EXPIRES_IN=7d

# Bcrypt
BCRYPT_SALT_ROUNDS=10

# Rate limit (auth)
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX=5

# CORS
CORS_ORIGIN=*
